#code
t = int(input())
while t>0:
    st = input()
    s = list(map(str, st.split(".")))
    for x in range(len(s)-1):
        s[x] = s[x][::-1]
        print(s[x], end=".")
    print(s[len(s)-1][::-1])     
    t-=1
